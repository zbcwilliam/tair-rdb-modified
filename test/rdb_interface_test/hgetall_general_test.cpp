#include "base_test.hpp"

//int hgetall(int area, data_entry &key, map<data_entry*, data_entry*> &field_values);

int main(int argc, char** argv) {
    testing::InitGoogleTest(&argc, argv);
    return RUN_ALL_TESTS();
}
